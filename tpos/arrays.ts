(() => {
    //const numbers:numbers[]
    //const numbers: (string | number | Boolean)[] = [1, 2, 3, 4, 5, '6'];
    const numbers: (number)[] = [1, 2, 3, 4, 5];

    //numbers.push(true);

    const villians = ['uno', 'dos', 'tres'];

    villians.forEach(v => console.log(v.toUpperCase()));

    console.log(numbers)


})()